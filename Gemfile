source "https://rubygems.org"

# Fastlane drives the iOS signing + TestFlight upload (fastlane/Fastfile, YT-99).
# Pinned to the 2.x line; `ruby/setup-ruby` with bundler-cache installs & caches it.
gem "fastlane", "~> 2.220"
